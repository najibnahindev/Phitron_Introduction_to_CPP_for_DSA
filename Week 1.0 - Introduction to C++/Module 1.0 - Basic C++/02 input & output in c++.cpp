#include<iostream>
using namespace std; //we can use std here so that we do not have to repeat
int main()
{
    int x;
    char a;
    double d;

    cin >> x >> a >> d;
    cout << x << " " << a << " " << d <<  endl;

    return 0;
}