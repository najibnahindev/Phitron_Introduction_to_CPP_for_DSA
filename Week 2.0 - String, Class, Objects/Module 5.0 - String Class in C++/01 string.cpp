#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s = "Hello";  //we can use string as data type
    cout << s << endl;

    s = "Gello";  //can directly change the input
    cout << s << endl;

    string s0 = "Gello";
    if(s == s0) cout << "Same";  //can directly compare
    else cout << "Not Same";


    return 0;
}